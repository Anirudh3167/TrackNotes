export interface LocalNotesType {
    noteId: string;
    access: string;
    content: string; // Just get the first 25 characters
    lastUpdated: string;
    createdAt: string;
    author: string;
  }